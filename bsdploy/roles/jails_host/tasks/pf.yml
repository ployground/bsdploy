---
- name: Enable pf in rc.conf
  service:
    name: pf
    enabled: yes
  notify: restart network

- name: Setup pf.conf
  template:
    src: pf.conf
    dest: /etc/pf.conf
  notify: restart pf
  tags: pf-conf

- name: Enable gateway in rc.conf
  service:
    name: gateway
    enabled: yes
  notify: restart network
